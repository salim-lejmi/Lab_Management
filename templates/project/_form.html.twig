{{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
           <a href="{{ path('new_equipment') }}">Create Equipment</a>
</div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
